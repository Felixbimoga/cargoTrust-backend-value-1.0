package com.gargotrust.gestion_achats_enligne.forwarder.domain.enums;

/** Fréquence des départs (groupages / conteneurs). */
public enum DepartureFrequency {
    HEBDOMADAIRE,
    BIMENSUEL,
    MENSUEL
}
